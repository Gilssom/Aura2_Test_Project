using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestBossMonster : TestMonster
{
    /*public enum BossType { Maze, ETC }
    public BossType m_BossType;

    public GameObject m_DropItem;

    public bool m_BossDeath;

    void Update()
    {
        if (isDeath && !m_BossDeath)
            BossDeath();
    }

    void BossDeath()
    {
        m_BossDeath = true;

        while (isDeath)
        {
            switch (m_BossType)
            {
                case BossType.Maze:
                    DropItem();
                    Debug.Log("½ºÅ³ È¹µæ °¡´É");
                    break;
                case BossType.ETC:
                    break;
                default:
                    break;
            }
        }
    }

    void DropItem()
    {
        GameObject Item = Instantiate(m_DropItem, this.transform.position, this.transform.rotation);
    }*/
}
